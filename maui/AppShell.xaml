<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MyApp.Maui.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MyApp.Maui"
    xmlns:views="clr-namespace:MyApp.Maui.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="MyApp">

    <!-- 🔹 Itt állítjuk be a menü viselkedését különböző eszközökön -->
    <Shell.FlyoutBehavior>
        <OnIdiom x:TypeArguments="FlyoutBehavior"
                 Phone="Flyout"
                 Tablet="Locked"
                 Desktop="Locked" />
    </Shell.FlyoutBehavior>

    <!-- A FlyoutItem adja a „főmenüt” -->
    <FlyoutItem Title="Főmenü" Route="main">

        <Tab Title="Vezérlőpult" Route="controlpanel">
            <ShellContent
                Title="Vezérlőpult"
                Route="controlpanel-page"
                ContentTemplate="{DataTemplate views:ControlPanelPage}" />
        </Tab>

        <Tab Title="Diákok" Route="students">
            <ShellContent
                Title="Diákok"
                Route="students-page"
                ContentTemplate="{DataTemplate views:StudentPage}" />
        </Tab>

        <Tab Title="Osztályok" Route="classes">
            <ShellContent
                Title="Osztályok"
                Route="classes-page"
                ContentTemplate="{DataTemplate views:SchoolClassesPage}" />
        </Tab>

    </FlyoutItem>

</Shell>
